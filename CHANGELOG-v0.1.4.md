# v0.1.4 更新说明

## 修复

### 本地 agent 用量统计虚高问题（约 50%）

**问题原因：**

Codex 的统计逻辑存在缺陷，将首个采样点的累计值误当作增量统计。当会话重启或从检查点恢复时，该累计值会被重复计算。

**修复内容：**

1. **修正 Codex 差分计算逻辑** (`electron/local-usage/codex.ts`)
   - 原逻辑：首个采样点值直接作为增量
   - 新逻辑：首个采样点仅作为基线，只统计相邻采样点的差值
   - 影响：频繁重启会话的用户统计将更准确

2. **添加缓存清理功能**
   - 新增 `electron/local-usage/clear-cache.ts` 模块
   - 在设置页面添加"清除缓存并重新扫描"按钮
   - 清除后会按新逻辑重新计算所有历史数据

**使用说明：**

1. 升级到 v0.1.4 后，打开应用
2. 进入"设置"页面
3. 找到"本地用量缓存"卡片
4. 点击"清除缓存并重新扫描"按钮
5. 确认操作后等待重新扫描完成（可能需要几秒到几分钟）
6. 重新查看用量和趋势页面，数据应已修正

**技术细节：**

修复前的统计逻辑：
```typescript
// 首个点当作增量（错误）
const delta = prev ? 
  { input: s.input - prev.input, ... } : 
  { input: s.input, ... };  // ❌ 会话重启时这里包含累计值
```

修复后的统计逻辑：
```typescript
// 首个点作为基线，跳过不计入统计（正确）
if (prev === null) {
  prev = s;
  continue;  // ✓ 只作为基线，不计入增量
}
const delta = {
  input: Math.max(0, s.input - prev.input), // ✓ 只计算差值
  ...
};
```

## 影响范围

- **影响的 agent**：Codex（其他 agent 不受影响）
- **影响用户**：频繁重启 Codex 会话的用户虚高幅度更明显
- **历史数据**：需要手动清除缓存重新扫描才能修正

## 测试

- ✅ 所有单元测试通过（155 个测试）
- ✅ TypeScript 类型检查通过
- ✅ 清除缓存功能正常工作
