# 变更日志

## 0.1.2

### 新增

- **诊断日志**：运行期间的错误会记到 userData 下的 `logs/token-lens.log`，
  「设置 → 诊断日志」可直接查看最近记录并一键定位文件。只写本地、无上传通道。
  写入层强制脱敏，API Key / Cookie / Authorization 均不会落盘。
- **崩溃不再白屏**：新增顶层错误边界，渲染异常时显示可读提示与错误摘要，
  而非整个窗口变白。渲染进程崩溃、页面加载失败、窗口无响应现均入日志。
- **价格表扩充到 6 家 80 余款模型**：新增 OpenAI GPT-5.6 Cyber / 5.5 / 5.4 /
  5.2 / 5 / 4.1 / 4o / o3 / o4-mini、Anthropic Fable 5 与 Opus 5 与 Sonnet 5 与
  Haiku 4.5、xAI Grok 4.6~4.20、智谱 GLM-5.3 与 GLM-5 全系、DeepSeek V4 Vision、
  小米 MiMo V2.5、Gemini 3.1 Pro 与各档 Flash-Lite。价格全部照各家官方定价页
  录入（核对日期 2026-08-25），统一折算为美元。
- **价格表正则顺序有了测试保护**：85 个用例锁死每个模型名命中哪一行。
  这类错配不会报错，界面照样显示金额，只是错的，靠肉眼审查发现不了。

### 修复

- **GPT-5.6 Sol 单价填成了 GPT-5.5 的价**：此前按 $5/$30 计费，官方促销价是
  $4/$20（至少持续到 2026-11-21），费用普遍高估约 22%。
- **Claude Opus 4.5 及以上被按 Opus 4 的价计费**：官方从 Opus 4.5 起降到
  $5/$25，而 Opus 4/4.1 仍是 $15/$75。宽松的 `opus-4` 正则把 4.6、4.8 一起
  吃掉，虚报整整 3 倍。
- **Claude Opus 5 / GLM-5.3 的用量此前完全没计费**：价格表里没有这两行，
  费用列一直显示「-」。实测补上后，仅本地历史记录就多统计出 $200 以上。
- **DeepSeek V4 的价格填的是小米 MiMo 的价**：两者恰好都曾录成 0.435/0.87。
  DeepSeek 官方峰时价为 Pro $1.32/$3.96、Flash $0.44/$1.32（谷时减半）。
- **内置表不再混用币种**：趋势页按 `cost` 直接求和，表里出现人民币行会把
  ¥ 和 $ 加在一起。Kimi K3 官方只给人民币价，现按汇率折算并在行内注明。
- **刷新时的「连接超时」不再误报故障**：undici（Node 内置 fetch）默认连接超时
  是 10 秒，且它先于应用自己的超时触发，导致设置页那个「请求超时」选项在连接
  阶段完全无效——调到 30 秒也照旧 10 秒断开。Kimi 的余额端点握手实测需要
  3~9 秒，正常但偏慢的请求被判成故障。现显式设为「用户配置值与 20 秒的较大者」。
- **Kimi 卡片不再因余额接口失败而整张报错**：原先只处理了 HTTP 状态码异常，
  网络异常会直接抛出、连 `/models` 兜底都进不去，卡片只剩一句
  `TypeError: fetch failed`，余额和 Key 状态全丢。现降级显示
  「Key 有效（余额接口连接失败）」，把原因写进标签。
- **错误日志会展开真实原因**：undici 把所有网络故障统一包成
  `TypeError: fetch failed`，真正的原因（`ECONNREFUSED` / `ENOTFOUND` /
  `UND_ERR_CONNECT_TIMEOUT`）藏在 `cause` 里。此前只取 message，日志上等于
  什么都没记。现逐层展开 cause 并附带错误码。
- **Gemini 的 API Key 不再出现在 URL 里**：改用 `x-goog-api-key` 请求头。
  此前 key 拼在 `?key=` 查询串，而请求超时的错误消息会带上完整 URL，
  两者叠加会把明文 key 带进错误提示和日志。
- **卡片徽章显示服务中文名**：此前显示的是 `scnet_token_plan` 这类原始 ID。
  名称是用户自填且切换服务类型时不会跟着变，徽章是唯一能看出这张卡查的是
  哪一家的地方，必须可读。
- **切换服务类型时名称跟着变**：此前先选「阿里云百炼」填好名称、再改选
  「超算互联网 Token Plan」，名称会留在原处，最终得到一张标题写「百炼」
  实际查超算互联网的卡片。用户自定义过的名称（如「工作号」）不受影响。

## 0.1.1

### 修复

- **硅基流动余额读错字段**：`/v1/user/info` 返回 `balance`（仅赠费余额）、
  `chargeBalance`（仅充值余额）、`totalBalance`（可用总额）三个字符串字段，
  此前只读 `balance`，导致充值 ¥88 的账号在卡片上显示 0.88。现取
  `totalBalance`，缺失时回退为赠费 + 充值求和，全取不到则不显示数字而非显示 NaN。
- **硅基流动货币单位标错**：适配器请求的是 `api.siliconflow.cn`（国内站，
  官网价目表通篇以 ¥ 计价），却硬编码为 USD。已改为 CNY。
- **Gemini / Groq / Together 不再被标为「免费服务」**：这三家均同时提供免费与
  付费档，查不到数字只是因为官方没有余额查询接口。现由适配器给出「Key 有效」
  并在副行说明原因，不再代替服务商断言账号是免费额度。
- **卡片副行文案与主位数字自相矛盾**：此前按 `used` 字段是否存在来判断服务有没有
  余额接口，而 DeepSeek / 硅基流动 / 火山方舟 / 百炼 / Kimi 只返回 `remaining`，
  于是这些服务会一边显示余额数字、一边写「该服务无余额查询 API」。
- **进度条遇脏数据渲染异常**：占比计算在 NaN / Infinity 时会把非法值直接写进
  CSS `width`。现返回 0——脏数据宁可渲染成空条，也不能渲染成满条谎称额度已用尽。

### 其他

- 引入 vitest，覆盖金额格式化、占比计算与硅基流动余额取值共 36 个用例。
- 修正 README 中对 Anthropic、OpenAI 的归类：两者官方接口给的是本月累计用量
  （token 数 / 花费），并非账户余额。

## 0.1.0

首个版本。
